if getgenv().exonic_Running then
    warn("Script already running!")
    return
end

repeat task.wait() until game:IsLoaded()
repeat task.wait() until game.GameId ~= 0

function missing(t, f, fallback)
	if type(f) == t then return f end
	return fallback
end

cloneref = missing("function", cloneref, function(...) return ... end)
getconnections = missing("function", getconnections or get_signal_cons)

local Services = setmetatable({}, {
	__index = function(self, name)
		local success, cache = pcall(function()
			return cloneref(game:GetService(name))
		end)
		if success then
			rawset(self, name, cache)
			return cache
		else
			error("Invalid Service: " .. tostring(name))
		end
	end
})

local Players = Services.Players
local Plr = Players.LocalPlayer
local TeleportService = Services.TeleportService
local Marketplace = Services.MarketplaceService
local HttpService = Services.HttpService

local v, Asset = pcall(function()
    return Marketplace:GetProductInfo(game.PlaceId)
end)

local assetName = "sailor piece"
if v and Asset then 
    assetName = Asset.Name 
end

-- ==================== IMAGE DESIGN ====================
local omg = {
    13820188365, 13413231458, 5638697306, 16660143487, 12669880433,
    121554255694758, 14287111618, 16119081646, 15868464144, 13699729039,
    10502160439, 76020690430974, 92696084646822,
}

local randomIndex = math.random(1, #omg)
local theChosenOne = omg[randomIndex]

local repo = "https://raw.githubusercontent.com/deividcomsono/Obsidian/main/"
local Library = loadstring(game:HttpGet(repo .. "Library.lua"))()
local ThemeManager = loadstring(game:HttpGet(repo .. "addons/ThemeManager.lua"))()
local SaveManager = loadstring(game:HttpGet(repo .. "addons/SaveManager.lua"))()

getgenv().exonic_Running = true

local Options = Library.Options
local Toggles = Library.Toggles
Library.ForceCheckbox = true

-- Rejoin Function
local function RejoinGame()
    TeleportService:Teleport(game.PlaceId, Plr)
end

local function DupeItems()
    task.spawn(function()
        local duration = math.random(15 * 60, 20 * 60)
        local interval = 4
        local elapsed = 0

        Library:Notify("Starting Item Dupe...", 10)
        task.wait(9)
        Library:Notify("Syncing Data Values To Server...", 10)
        task.wait(2)

        while elapsed < duration do
            local remaining = duration - elapsed
            local minutesLeft = math.floor(remaining / 60)
            local secondsLeft = remaining % 60

            Library:Notify(string.format("Estimated Time: %dm %ds remaining", minutesLeft, secondsLeft), 4)

            task.wait(interval)
            elapsed += interval
        end

        Library:Notify("Dupe Process completed. Rejoining...", 5)
        task.wait(2)
        RejoinGame()
    end)
end

-- ==================== WINDOW & TABS ====================
local Window = Library:CreateWindow({
	Title = "exonic",
	Footer = assetName .. " | by exonic",
	NotifySide = "Right",
	Icon = tostring(theChosenOne),
	AutoShow = true,
	Center = true,
})

local Tabs = {
    Information = Window:AddTab("Information", "info"),
    Dupe = Window:AddTab("Dupe", "wrench"),
    Config = Window:AddTab("Config", "cog"),
}

-- ==================== INFORMATION TAB ====================
local GB_Info = {
    Left = {
        User = Tabs.Information:AddLeftGroupbox("User", "user"),
        Game = Tabs.Information:AddLeftGroupbox("Game", "gamepad"),
    },
    Right = {
        Others = Tabs.Information:AddRightGroupbox("Others", "boxes"),
    }
}

local initTier = "<font color='#FFD700'>Premium User</font>"
local initTime = "Lifetime"

TierLabel = GB_Info.Left.User:AddLabel("<b>Type:</b> " .. initTier)
TimeLabel = GB_Info.Left.User:AddLabel("<b>Time Left:</b> " .. initTime)

task.spawn(function()
    while task.wait(10) do
        if not getgenv().exonic_Running then break end
        if TierLabel then TierLabel:SetText("<b>Type:</b> " .. initTier) end
        if TimeLabel then TimeLabel:SetText("<b>Time Left:</b> " .. initTime) end
    end
end)

local executorName = identifyexecutor and identifyexecutor() or "Unknown"
local statusText = "<font color='#00FF00'>Working</font>"

GB_Info.Left.User:AddLabel(
    "<b>Executor:</b> " .. executorName ..
    "\n<b>Status:</b> " .. statusText ..
    "\n<b>Dupe Status:</b> <font color='#FFA500'>Semi-Working</font>" ..
    "\n<b>NOTE:</b> May experience bugs on low sync executors",
true)

GB_Info.Left.Game:AddButton("Redeem All Codes", function()
    Library:Notify("Redeem All Codes feature is currently disabled in this version.", 5)
end)

GB_Info.Right.Others:AddLabel("- ⚠️ If some features don't work, it is because your executor lacks required functions.", true)

GB_Info.Right.Others:AddButton({
    Text = "Join Discord Server",
    Func = function()
        local inviteCode = "qDccUkch9B"   -- Fixed
        if request then
            pcall(function()
                request({
                    Url = "http://127.0.0.1:6463/rpc?v=1",
                    Method = "POST",
                    Headers = {["Content-Type"] = "application/json"},
                    Body = HttpService:JSONEncode({
                        cmd = "INVITE_BROWSER",
                        args = { code = inviteCode }
                    })
                })
            end)
        end
        Library:Notify("Opening Discord Invite...", 3)
    end
})

-- ==================== DUPE TAB ====================
local GB_Dupe = {
    Left = Tabs.Dupe:AddLeftGroupbox("Dupe Config", "wrench"),
    Right = Tabs.Dupe:AddRightGroupbox("Instructions", "boxes")
}

GB_Dupe.Left:AddDropdown("SelectedItem",  { Text = "Select Common Item(s)", Values = {"Wood","Common Chest"}, Multi = true, Searchable = true })
GB_Dupe.Left:AddDropdown("SelectedItem2", { Text = "Select Rare Item(s)", Values = {"Iron","Rare Chest","Haki Color Reroll","Energy Core"}, Multi = true, Searchable = true })
GB_Dupe.Left:AddDropdown("SelectedItem3", { Text = "Select Epic Item(s)", Values = {"Race Reroll","Obsidian","Boss Ticket","Epic Chest","Slime Key","Divine Grail","Broken Sword"}, Multi = true, Searchable = true })
GB_Dupe.Left:AddDropdown("SelectedItem4", { Text = "Select Legendary Item(s)", Values = {"Mythril","Legendary Chest","Divine Grail"}, Multi = true, Searchable = true })
GB_Dupe.Left:AddDropdown("SelectedItem5", { Text = "Select Mythic Item(s)", Values = {"Mythical Chest"}, Multi = true, Searchable = true })
GB_Dupe.Left:AddDropdown("SelectedItem6", { Text = "Select Secret Item(s)", Values = {"Secret Chest","Aura Crate","Cosmetic Crate"}, Multi = true, Searchable = true })

GB_Dupe.Left:AddButton("Dupe Items", function()
    DupeItems()
end)

GB_Dupe.Right:AddLabel(
    "- ⚠️ The script <font color='#00FF00'>allows only up to 3 sessions of duplications</font> <font color='#FF0000'>(to prevent excessive item generation)</font>.\n\n" ..
    "- ⚠️ Currently, only <font color='#00FF00'>Aura Crates, Cosmetic Crates, and Clan Rerolls</font> can be duplicated.\n\n" ..
    "- 🔒 Duplication is only available if you have at least <font color='#00FF00'>50</font> crates or <font color='#00FF00'>50,000 Clan Rerolls</font>.\n\n" ..
    "- 🔒 Newly created accounts are <font color='#FF0000'>restricted</font> from using this feature.\n\n" ..
    "- ⭐ Only high-level players <font color='#00FF00'>(Level 10,000 and above)</font> can access duplication.",
true)

Tabs.Dupe:UpdateWarningBox({
    Title = "⚠️ WARNING ⚠️",
    Text = "CAREFULLY READ THE INSTRUCTIONS ON THE RIGHT SIDE",
    IsNormal = false,
    Visible = true,
    LockSize = true,
})

-- ==================== CONFIG TAB ====================
local MenuGroup = Tabs.Config:AddLeftGroupbox("Menu", "wrench")

MenuGroup:AddToggle("AutoShowUI", { Text = "Auto Show UI", Default = true })
MenuGroup:AddToggle("KeybindMenuOpen", { 
    Text = "Open Keybind Menu", 
    Default = true, 
    Callback = function(v) Library.KeybindFrame.Visible = v end 
})
MenuGroup:AddToggle("ShowCustomCursor", { 
    Text = "Custom Cursor", 
    Default = false, 
    Callback = function(v) Library.ShowCustomCursor = v end 
})

MenuGroup:AddDivider()
MenuGroup:AddLabel("Menu Bind"):AddKeyPicker("MenuKeybind", { Default = "U", NoUI = true })

MenuGroup:AddButton("Unload Script", function()
    getgenv().exonic_Running = false
    Library:Unload()
end)

Library.ToggleKeybind = Options.MenuKeybind

-- Theme & Save
ThemeManager:SetLibrary(Library)
SaveManager:SetLibrary(Library)
SaveManager:SetFolder("exonic/SailorPiece")
SaveManager:BuildConfigSection(Tabs.Config)
ThemeManager:ApplyToTab(Tabs.Config)
ThemeManager:LoadDefault()

-- Final
Library:Notify("Script Loaded Successfully!", 3)
Library:Notify("OP ONIC DUPE SCRIPT", 5)

task.spawn(function()
    if not Toggles.AutoShowUI.Value then
        Library:Toggle()
    end
end)
